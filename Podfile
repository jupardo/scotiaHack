# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'com.randomName' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for com.randomName

  pod 'PopupDialog', '~> 0.8'

  target 'com.randomNameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'com.randomNameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
